alert("Pakka... ???");

function clearvalue() {
  result.value = 0;
}
function fn(a) {
  if (result.value == 0) {
    result.value = a;
  } else {
    result.value = result.value + a;
  }
}
function per() {
  result.value = result.value + "%";
}

let percentage = (result.value * result.value) / 100;
function ans() {
  result.value = eval(result.value);
}

function ans() {
  result.value = "Main Nahi Bta Rha";
}
